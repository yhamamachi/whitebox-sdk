name: Build S4SK
run-name: Build S4SK
on:
  push:
    branches:
      - 'v5.x-migration'
  workflow_dispatch:

jobs:
  Build:
    name: Build
    if: github.repository_owner == 'yhamamachi'
    uses: ./.github/workflows/build_ca55.yaml
    with:
      TARGET_BOARD: "s4sk"

